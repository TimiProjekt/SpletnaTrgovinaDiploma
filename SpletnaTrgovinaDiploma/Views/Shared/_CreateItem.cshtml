@using SpletnaTrgovinaDiploma.Helpers
@model string;

@if (User.IsUserAdmin())
{
    <div style="position:fixed; right: 25px; bottom: 75px;" class="text-white">
        <a asp-controller="@Model" asp-action="Create" class="btn btn-success">
            <i class="badge-info bi-plus-circle"></i> Add New
        </a>
    </div>
}